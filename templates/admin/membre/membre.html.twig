{% extends "base.html.twig" %}

{% block title %}Backoffice | Inscription{% endblock %}

{% block body %}
	

	<div class="row">
		<div class="col-4 mx-auto">
			{{ form_start(form) }}

			<div class="row">
				<div class="col-6">{{ form_row(form.prenom) }}</div>
				<div class="col-6">{{ form_row(form.nom) }}</div>
			</div>

			{{ form_end(form) }}
		</div>
	</div>
{% endblock %}
