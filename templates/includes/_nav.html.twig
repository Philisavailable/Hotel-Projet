<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('show_home') }}">HouseHotel</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarNavDropdown">
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link active" aria-current="page" href="{{ path('show_home') }}">{% trans %}Accueil{% endtrans %}</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('show_chambres') }}">{% trans %}Chambres{% endtrans %}</a>
					{# {{ path('show_room') }} #}
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('show_restaurant') }}">Restaurant</a>
					{# {{ path('show_restaurant') }} #}

				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('show_spa')}}">Spa</a>
					{# {{ path('show_spa') }} #}

				</li>
				<li class="nav-item">
					<a class="nav-link" href="#">Reservation</a>
					{# {{ path('show_reservation') }} #}

				</li>
				<li class="nav-item">
					<a class="nav-link" href="#">{% trans %}Avis{% endtrans %}</a>
					{# {{ path('show_notice') }} #}

				</li>

				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">Hotel</a>
					<ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
						<li>
							<a class="dropdown-item" href="{{ path('presentation_hotel') }}">{% trans %}Qui sommes-nous{% endtrans %} ?</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{ path('acces_hotel') }}">{% trans %}Accès{% endtrans %}</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{ path('contact_hotel') }}">{% trans %}Contact{% endtrans %}</a>
						</li>
					</ul>
				</li>

				<li class="nav-item">
					<a class="nav-link" href="#">{% trans %}Actualités{% endtrans %}</a>
					{# {{ path('show_actuality') }} #}

				</li>
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">Admin</a>
					<ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
						<li>
							<a class="dropdown-item" href="{{ path('create_chambre') }}">Chambre</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{ path('app_slider_controler') }}">Slider</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{ path('show_membres') }}">Membre</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{ path('show_commande') }}">Commande</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{ path('create_actualite') }}">Actualites</a>
						</li>
					</ul>
				</li>
			</ul>
			<div class="me-5">
					<!-- {{ dump(locales) }} -->
					{% for locale in locales %}
						<a href="{{ path('change_locale', {'locale': locale}) }}" class="btn-sm ms-auto text-decoration-none text-dark">{{ locale }}</a>
					{% endfor %}
			</div>
		</div>
	</div>
</nav>
